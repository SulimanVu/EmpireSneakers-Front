import { FC } from "react";
import styles from "./authorization.module.scss";

const Authorization: FC = () => {
  return <div className={styles.auth}>Authorization</div>;
};

export default Authorization;
